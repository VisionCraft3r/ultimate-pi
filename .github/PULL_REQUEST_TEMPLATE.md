## Summary

What this PR changes and why.

## Test plan

How a reviewer can verify the change.

## Checklist

- [ ] `npm test` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run scan` passes (`scripts/scan-secrets.mjs` and `scripts/scan-personal.mjs`)
- [ ] No secrets, API keys, `auth.json`, `.env`, or personal filesystem paths
